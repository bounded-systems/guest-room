\* TLC model for Confinement.tla — a small finite instance that is exhaustively
\* model-checked. Run:  java -cp tla2tools.jar tlc2.TLC -config Confinement.cfg Confinement.tla
SPECIFICATION Spec

CONSTANTS
    Caps = {reads}
    CaveatU = {r1, r2}
    Consumers = {c1}
    MaxTime = 3
    Leases = {1, 2}

INVARIANT TypeOK
INVARIANT Confinement
